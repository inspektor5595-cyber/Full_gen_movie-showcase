# 🚀 Автоматизированный AI-Пайплайн 


## 📌 О проекте
Полностью автоматизированный пайплайн на Python для превращения сырых текстовых сценариев в готовые смонтированные, озвученные и анимированные видеоролики. 

Проект демонстрирует навыки интеграции сложных API, веб-автоматизации (Playwright), работы с многопоточностью, обработки ошибок и разработки с использованием ИИ (Vibe Coding)**. Система работает как отказоустойчивый оркестратор, связывающий сразу несколько нейросетей (Gemini, ElevenLabs, Google ImageFX, Grok) в единый непрерывный процесс.

## 🛠 Стек технологий и инструменты
* **Core:** Python 3, Threading (Многопоточность), Subprocess, работа с очередями (Queue).
* **Web Automation:** Playwright, интеграция с антидетект-браузерами (OctoBrowser API), решение капчи (Anti-Captcha).
* **Media Processing:** FFmpeg (сложные графы фильтров, склейка, программная синхронизация видео и аудио).
* **AI & LLMs:** Gemini 2.5 Flash (промпт-инжиниринг, семантический анализ текста), ElevenLabs API, Google Labs (WhiskBot API), Grok (xAI).
* **Infrastructure:** Динамическая ротация прокси, управление токенами, контроль состояния (State Management).

---

## ⚙️ Архитектура и этапы работы

Система модульная и управляется через центральный пульт `config.py`, что обеспечивает строгое разделение логики на три независимых этапа:

### Этап 1: Генерация ассетов и умная нарезка (`el11_15_rotate.py`)
1. **Семантический парсинг:** Использует Gemini для анализа сценария, извлечения метаданных персонажей, определения визуальных сцен и "умной" разбивки текста на кадры (`[CUT]`).
2. **Генерация аудио:** Интеграция с ElevenLabs API. Включает автоматическую ротацию токенов, обход авторизации (Firebase Auth) и систему "Экстренного рерайта текста", если жесткие фильтры безопасности блокируют генерацию.
3. **Генерация изображений:** Автоматизация Google ImageFX через реверс-инжиниринг недокументированного API (WhiskBot). Обеспечивает программную загрузку референсов (стиль/герои) для сохранения визуальной консистентности кадров.

### Этап 2: Видео-анимация (`Grok8.py`)
1. **Продвинутая веб-автоматизация:** Использование **Playwright** в связке с профилями OctoBrowser для автоматизации генерации видео в Grok (X.com).
2. **Динамический парсинг DOM:** Интеллектуальная обработка A/B тестов интерфейса (скрипт сам понимает, какой дизайн загрузился, обрабатывает всплывающие окна и отключает авто-генерацию на лету).
3. **Отказоустойчивость и лимиты:** Внедрена кастомная система очередей. Если аккаунт ловит жесткий лимит (Rate Limit) или сложную капчу, воркер безопасно останавливается, меняет аккаунт и продолжает задачу без потери данных.

### Этап 3: Умный монтаж и рендер (`montajj.py`)
1. **Оркестрация FFmpeg:** Автоматическое вычисление длительности аудиодорожек через `ffprobe`.
2. **Динамическая синхронизация:** Если сгенерированное видео короче аудио, скрипт динамически пересчитывает PTS (замедляет видео), чтобы идеально подогнать его под длину голоса диктора.
3. **Сборка:** Программное применение паддингов (padding), масштабирования (scale) и переходов, на выходе — готовый `.mp4` файл.

---

## 🎯 Навыки для QA Automation Engineering

Этот проект создавался с упором на инженерную культуру, принципы контроля качества и надежной автоматизации:

* **Self-Healing (Самовосстановление):** Все сетевые запросы и взаимодействия с DOM обернуты в надежные блоки `try/except` с логикой экспоненциальной задержки и повторных попыток (retries). 
* **State Management (Умное возобновление):** Пайплайн логирует каждый свой шаг. При сбое (отключение света, падение серверов, бан прокси) скрипт возобновляет работу ровно с того места, где остановился, проверяя целостность файлов (`file_size > 1000 bytes`).
* **Fallback-стратегии:** Если ИИ-модель отказывается генерировать картинку из-за цензуры (кровь/взрывы), система автоматически вызывает другую LLM для программной "нейтрализации" промпта и безопасного обхода фильтров.
* **Многопоточность (Concurrency):** Использование `ThreadPoolExecutor` для параллельной генерации десятков медиафайлов с жестким соблюдением лимитов сторонних API (Rate Limits) и блокировками потоков (Locks).

## 👨‍💻 Об авторе
Я Python-инженер и AI-специалист. Фокусируюсь на оптимизации процессов, архитектуре пайплайнов и решении нестандартных технических задач. Ищу позицию **QA Automation Engineer**, где смогу применить свой опыт автоматизации сложных процессов, навыки тестирования логики и владение Python для обеспечения стабильности и высокого качества продукта.
